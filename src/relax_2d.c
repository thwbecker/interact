/* 


   program to test visco-elastic relaxation via effective modulus

*/

int main(void)
{
  struct flt *fault;
  struct med *medium;
  read_geometry("geom.in",&medium,&fault,FALSE,FALSE,TRUE);
  
  
  islip = 0;			/* first one slides */
  for(j=1;j<medium->nrflt;i++)
  return 0;
}
